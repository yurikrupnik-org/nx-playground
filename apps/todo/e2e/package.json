{
  "name": "todo-e2e",
  "version": "0.0.1",
  "description": "Playwright end-to-end suite for the todo vertical: one todo-api, every delivery option (Solid SPA, Astro SSR island + htmx, axum + htmx, SSE realtime, gRPC).",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --write ."
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "26.5.1",
    "typescript": "^7.0.2"
  }
}
