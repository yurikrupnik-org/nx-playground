# GENERATED by `butler tilt gen` — DO NOT EDIT BY HAND.
# Config: apps/todo/web/butler.toml (app) + butler.toml (repo root). Image inputs: repo convention [imageDefaults]

# todo-web

local_resource(
    'build-todo-web',
    cmd='bun nx run todo-web:build',
    dir='../../..',
    deps=['src', 'project.json', 'vite.config.ts', 'index.html'],
    ignore=['k8s'],
    labels=['bun'],
)

docker_build(
    'yurikrupnik/todo-web',
    build_args={'DIST_PATH': 'apps/todo/web/dist'},
    context='../../..',
    dockerfile='../../../manifests/dockers/Dockerfile',
    only=[
        'apps/todo/web/dist',
        'manifests/dockers/default.conf.template',
    ],
    target='nginx',
)

k8s_yaml(local('kcl run "oci://docker.io/yurikrupnik/app?tag=0.1.2" -D env=dev -q', dir='k8s'))
k8s_resource('todo-web', resource_deps=['build-todo-web'])
