# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/terran/web/butler.toml [env.prod] — diffs the package deep-merges over values.yaml.
workload:
  extraManifests:
  - apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    metadata:
      name: terran-web
      namespace: terran
    spec:
      hostnames:
      - terran.${GATEWAY_SUFFIX}
      parentRefs:
      - name: main-gateway
        namespace: gateway
        sectionName: https
      rules:
      - backendRefs:
        - name: terran-web
          port: 8080
        matches:
        - path:
            type: PathPrefix
            value: /
  image: yurikrupnik/terran-web:main
