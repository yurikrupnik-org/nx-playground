# GENERATED by `tilt-orch gen` — DO NOT EDIT BY HAND.
# Build inputs come from this app's project.json `container` target.
# Orchestration overrides: manifests/tilt/tilt.toml. k8s: ./k8s (KCL, rendered live).

# terran-web
local_resource(
    'build-terran-web',
    cmd='bun nx run terran-web:build',
    dir='../../..',
    deps=['src', 'project.json', 'vite.config.ts'],
    ignore=['k8s'],
    labels=['bun'],
)

docker_build(
    'yurikrupnik/terran-web',
    build_args={'DIST_PATH': 'apps/terran/web/dist'},
    context='../../..',
    dockerfile='../../../manifests/dockers/Dockerfile',
    target='nginx',
)
k8s_yaml(local('kcl run k8s -D env=dev -D image=yurikrupnik/terran-web -D namespace=terran', quiet=True))
k8s_resource('terran-web', port_forwards='5230:8080', resource_deps=['build-terran-web'])
