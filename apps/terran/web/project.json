{
  "name": "terran-web",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/terran/web/src",
  "targets": {
    "container": {
      "executor": "@nx-tools/nx-container:build",
      "dependsOn": ["build"],
      "options": {
        "file": "manifests/dockers/Dockerfile",
        "context": ".",
        "build-args": ["DIST_PATH=apps/terran/web/dist"],
        "tags": ["$REGISTRY/terran-web:latest"],
        "push": false
      },
      "configurations": {
        "ci": {
          "push": true,
          "metadata": {
            "images": ["$REGISTRY/terran-web"],
            "tags": [
              "type=sha",
              "type=ref,event=branch",
              "type=ref,event=pr",
              "type=raw,value=$APP_VERSION,enable=$ENABLE_VERSION"
            ]
          }
        }
      }
    },
    "scan": {
      "executor": "nx:run-commands",
      "dependsOn": ["container"],
      "options": {
        "command": "trivy image $REGISTRY/terran-web:latest --severity CRITICAL,HIGH --exit-code 0",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {
        "ci": {
          "command": "trivy image $REGISTRY/terran-web:sha-$(echo $SHORT_SHA | cut -c1-7) --severity CRITICAL,HIGH --format sarif --output trivy-terran-web.sarif"
        }
      }
    }
  },
  "metadata": {
    "tilt": {
      "portForwards": "5230:8080"
    }
  },
  "tags": []
}
