apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: zerg

resources:
  - ../../../base
  - scaledobject.yaml

namePrefix: neo4j-

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: zerg-db-worker-config
    literals:
      - DB_BACKEND=neo4j
      - APP_PORT=8081
      - HEALTH_PORT=8081
      - NEO4J_URL=bolt://neo4j.dbs.svc.cluster.local:7687

patches:
  - patch: |-
      - op: replace
        path: /spec/template/metadata/annotations/dapr.io~1app-id
        value: "db-worker-neo4j"
      - op: replace
        path: /spec/template/spec/containers/0/resources
        value:
          requests:
            memory: "128Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
    target:
      kind: Deployment
      name: zerg-db-worker
