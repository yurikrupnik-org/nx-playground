# GENERATED by `tilt-orch gen` — DO NOT EDIT BY HAND.
# Build inputs come from this app's project.json `container` target.
# Orchestration overrides: manifests/tilt/tilt.toml. k8s: kustomize overlays/local.

# zerg-api
docker_build(
    'yurikrupnik/zerg-api',
    build_args={'APP_NAME': 'zerg_api'},
    context='../../..',
    dockerfile='../../../manifests/dockers/rust.Dockerfile',
    only=[
        'Cargo.toml',
        'Cargo.lock',
        'apps/',
        'libs/',
        'manifests/dockers/rust.Dockerfile',
    ],
    ignore=[
        'target/',
        '**/*.md',
        '**/.git/',
        '**/k8s/',
        '**/Tiltfile',
        'apps/**/node_modules/',
    ],
    live_update=[
        sync('src', '/app/apps/zerg/api/src'),
    ],
    target='rust',
)
k8s_yaml(kustomize('k8s/kustomize/overlays/dev'))
k8s_resource('zerg-api', port_forwards='5221:8080', labels=['backend'], resource_deps=['zerg-shared-config'])
