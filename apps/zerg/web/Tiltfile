# GENERATED by `butler tilt gen` — DO NOT EDIT BY HAND.
# Config: apps/zerg/web/butler.toml (app) + butler.toml (repo root). Image inputs: repo convention [imageDefaults]

# zerg-web

local_resource(
    'build-zerg-web',
    cmd='bun nx run zerg-web:build',
    dir='../../..',
    deps=['src', 'project.json', 'vite.config.ts', 'index.html'],
    ignore=['k8s'],
    labels=['bun'],
)

docker_build(
    'yurikrupnik/zerg-web',
    build_args={'DIST_PATH': 'apps/zerg/web/dist'},
    context='../../..',
    dockerfile='../../../manifests/dockers/Dockerfile',
    only=[
        'apps/zerg/web/dist',
        'manifests/dockers/default.conf.template',
    ],
    target='nginx',
)

k8s_yaml(local('kcl run "oci://docker.io/yurikrupnik/app?tag=0.1.2" -D env=dev -q', dir='k8s'))
k8s_resource('zerg-web', port_forwards='5206:8080', resource_deps=['build-zerg-web'])
