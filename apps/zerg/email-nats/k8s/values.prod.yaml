# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/zerg/email-nats/butler.toml [env.prod] — diffs the package deep-merges over values.yaml.
workload:
  image: yurikrupnik/zerg-email-nats:main
  resources:
    limits:
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 128Mi
  scaler:
    behavior:
      scaleDown:
        policies:
        - periodSeconds: 30
          type: Percent
          value: 50
        stabilizationWindowSeconds: 60
      scaleUp:
        policies:
        - periodSeconds: 15
          type: Pods
          value: 5
        stabilizationWindowSeconds: 0
    cooldownPeriod: 60
    maxReplicas: 10
    minReplicas: 0
    pollingInterval: 5
    triggers:
    - metadata:
        account: $G
        activationLagThreshold: '1'
        consumer: email-worker
        lagThreshold: '10'
        natsServerMonitoringEndpoint: nats.dbs.svc.cluster.local:8222
        stream: EMAILS
      type: nats-jetstream
config:
  SENDGRID_FROM_NAME: Zerg
