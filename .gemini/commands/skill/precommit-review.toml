# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/precommit-review/SKILL.md (edit there, then run `just agents-gen`)
description = 'Two-pass review of a staged change before it is committed — pass 1 by the model that wrote the code, pass 2 by independent components (reviewer + security-reviewer subagents, CodeRabbit CLI, Codex CLI) that never see pass 1. Use before any commit of non-trivial code, when asked to review changes / "review before commit" / run /precommit, or when a change is about to go into a PR.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: The model that wrote the code knows the intent and therefore forgives itself. The fix is two passes with different blind spots over ONE frozen input: the staged index, not anyone's memory of the edit. The bundle is also the only thing that stops a secret-shaped path reaching a vendor CLI — `coderabbit` and `codex` transmit the WORKING TREE, not the index.
Verification: run `just review-bundle` and report its output. Nothing is done until it passes.

@{.claude/skills/precommit-review/SKILL.md}

Task: {{args}}
"""
