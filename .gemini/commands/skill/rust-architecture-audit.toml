# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/rust-architecture-audit/SKILL.md (edit there, then run `task agents-gen`)
description = "Audit where Rust logic lives and how it is managed across this workspace's crate layers (apps → domains → core/contracts) — layer placement, anemic models, logic leaking into apps or handlers, cross-domain coupling, service-boundary hygiene, duplication. Use when asked to review/check/audit the architecture, \"where should this logic go\", \"is this in the right crate/layer\", before extracting a service, or when a PR adds business rules, SQL, or a domain dependency to an app."
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: Every shortcut in a layered workspace is locally reasonable: one SQL query in a handler, one `if status ==` in a service, one domain crate depending on its neighbour. Each is invisible in review and permanent in effect. The audit turns placement into findings tied to a command that fails when the finding is ignored, so \"where should this logic go\" has a checkable answer instead of a preference.
Verification: run `task boundaries` and report its output. Nothing is done until it passes.

@{.claude/skills/rust-architecture-audit/SKILL.md}

Task: {{args}}
"""
