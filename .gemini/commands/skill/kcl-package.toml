# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/kcl-package/SKILL.md (edit there, then run `just agents-gen`)
description = "Create, version and publish a KCL package as an OCI artifact to a registry of the user's choice (Docker Hub, ghcr.io, a local kind registry), and consume it from this repo. Use when adding or bumping a KCL package, changing `[k8s] package`/`tag` in butler.toml, or when `kcl mod push`/`kcl run oci://` fails."
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: Manifests are rendered by a PINNED KCL package that lives in a SIBLING repo and is consumed as an OCI artifact. The intuitive move — write the `.k` files next to the app — was tried (`apps/terran/*/k8s`) and produced `CannotFindModule`. Publishing and bumping the package is a cross-repo procedure with a registry, a tag and a consumer pin, none of which is discoverable from this repo alone.
Verification: run `just k8s-check` and report its output. Nothing is done until it passes.

@{.claude/skills/kcl-package/SKILL.md}

Task: {{args}}
"""
