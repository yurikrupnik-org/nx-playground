# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/deps-maintenance/SKILL.md (edit there, then run `just agents-gen`)
description = 'Update dependencies or diagnose a failed upkg run. Use when the user asks to update/upgrade deps, runs just upkg/weekly, or pastes output containing BEGIN-UPKG-DIAGNOSIS or OSV-Scanner failures.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: `cargo upgrade --incompatible` is the obvious command and the wrong one: it bulldozes deliberate range pins — `testcontainers = \"=0.27.3\"` is exact-pinned precisely so it is skipped — and it runs no OSV scan, no npm cooldown and no post-check. Every update therefore goes through `upkg`, and a failed run prints a diagnosis block that is only actionable if you know how to read it.
Verification: run `just check (post-upkg) + just scan` and report its output. Nothing is done until it passes.

@{.claude/skills/deps-maintenance/SKILL.md}

Task: {{args}}
"""
