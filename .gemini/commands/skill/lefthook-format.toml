# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/lefthook-format/SKILL.md (edit there, then run `task agents-gen`)
description = 'Wire formatters into git hooks with lefthook — staged-file fixers on pre-commit, read-only gates on pre-push — calling the formatter binary directly, through `task`, through `just`, or any shell command. Use when editing lefthook.yml, adding/changing a formatter in a hook, when a hook rewrites files you did not stage, or when hooks do not run at all.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: The obvious hook — run the whole-tree formatter and `stage_fixed` the result — commits only the staged files but rewrites every other file in the working tree, reformatting someone's unrelated WIP. The per-file forms each have a trap the task leaves never hit: bare `rustfmt` formats as edition 2015, explicit paths make `typos --write-changes` ignore `_typos.toml` excludes and rewrite vendored htmx, and `just`'s `{{files}}` splits a path with a space. Exported to local runtimes only: git hooks run in a checkout.
Verification: run `task hooks-check` and report its output. Nothing is done until it passes.

@{.claude/skills/lefthook-format/SKILL.md}

Task: {{args}}
"""
