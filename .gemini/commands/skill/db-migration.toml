# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/db-migration/SKILL.md (edit there, then run `task agents-gen`)
description = 'Add or modify a database schema migration (Atlas versioned mode). Use when changing table schemas, adding columns/indexes, or when the user mentions migrations, schema.sql, or AtlasMigration.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: One schema change is four artifacts that must agree: the timestamped `.up.sql`, `schema.sql`, `atlas.sum` and the migrations ConfigMap. Only the first is obvious from the request, and the failure mode of skipping the re-hash is invisible locally — a stale `atlas.sum` breaks the Atlas Operator at apply time, in the cluster, long after the PR went green.
Verification: run `task migrate-validate DB=<db>` and report its output. Nothing is done until it passes.

@{.claude/skills/db-migration/SKILL.md}

Task: {{args}}
"""
