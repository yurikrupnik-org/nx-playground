# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/service-transport/SKILL.md (edit there, then run `task agents-gen`)
description = 'Choose and wire a communication surface between processes — NATS JetStream topics/event architecture, gRPC, or plain HTTP/REST (plus Postgres NOTIFY for browser realtime). Use when adding a stream/subject/consumer/worker, a proto service or gRPC client, a REST endpoint or outbound HTTP call, or when asked how service A should talk to service B.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: \"How should A talk to B\" is answered by the repo, not by taste: one broker (NATS JetStream), one proto module, one HTTP toolkit, Postgres NOTIFY for browser realtime — Kafka and RabbitMQ are already `status = \"rejected\"`. Without this, an agent adds a second broker or a second gRPC binary over tables an existing service already serves on one port, and the cost is a permanent transport to maintain.
Verification: run `task proto-lint` and report its output. Nothing is done until it passes.

@{.claude/skills/service-transport/SKILL.md}

Task: {{args}}
"""
