# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/new-rust-crate/SKILL.md (edit there, then run `just agents-gen`)
description = 'Scaffold a new Rust crate (lib or app) in this workspace. Use when adding a domain lib, core lib, contract crate, or a new service/API binary.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: `cargo new` is wrong here in four ways at once: dependency versions live only in the root `[workspace.dependencies]`, `members` is an explicit alphabetical list and not a glob, the layer a crate lands in decides what it may depend on, and a hand-written `project.json` silently SHADOWS the nx targets the local plugin infers — 27 such files were deleted for that reason. A crate scaffolded by instinct compiles and still breaks the graph.
Verification: run `just lint-rust` and report its output. Nothing is done until it passes.

@{.claude/skills/new-rust-crate/SKILL.md}

Task: {{args}}
"""
