# GENERATED by tools/agents/gen.ts from docs/agents/registry.toml — do not edit
# Skill body: .claude/skills/cncf-manager/SKILL.md (edit there, then run `task agents-gen`)
description = 'Evidence-driven evaluation, adoption, drift-audit and retirement of infrastructure/CNCF-landscape tooling (mesh, gateway, gitops, operators, scanners, SaaS). Human approval is REQUIRED before anything paid. Use when asked "do we actually use X?", when adding/replacing/removing a platform tool or operator, when a tool appears in docs but maybe not in the cluster, or when a change introduces a paid plan, seat, quota or SaaS token.'
prompt = """
You are working in the nx-playground monorepo. The skill below is the repo's own procedure, not a suggestion — follow it literally, and if it conflicts with your instinct, it wins.

Why this skill exists: Prose is not proof. README.md listed Istio + Kiali as prerequisites for months while the repo contained no control plane, no CR and no canary — an agent reading that prose would have \"kept\" a mesh that never existed, or installed one to make the docs true. This skill makes adoption a claim the repo can reproduce (install path + consumer + gate) and makes anything paid stop for a human signature, which is the one decision an agent must never take alone.
Verification: run `task tooling-check` and report its output. Nothing is done until it passes.

@{.claude/skills/cncf-manager/SKILL.md}

Task: {{args}}
"""
