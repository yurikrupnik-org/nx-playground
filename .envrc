# Direnv configuration for nx-playground
# Automatically loads environment variables when you cd into this directory
#
# Setup:
#   1. Install direnv: https://direnv.net
#      brew install direnv  # macOS
#      apt install direnv   # Ubuntu/Debian
#   2. Add to your shell rc file (~/.zshrc, ~/.bashrc):
#      eval "$(direnv hook zsh)"  # or bash
#   3. Copy .env.example to .env and fill in your values
#   4. Run: direnv allow
#
# This file is safe to commit (no secrets here!)

# Load environment variables from .env file (which is gitignore)
dotenv_if_exists .env
# env_vars_required DB_ENDPOINT

# SeaORM CLI configuration
export MIGRATION_DIR=libs/migration
export DATABASE_SCHEMA=public
# Application configuration
export RUST_LOG=info
export RUST_ENV=development
# export APP_ENV=production
export APP_ENV=development
export PORT=8080
# Show that environment was loaded
echo "✓ direnv: Environment loaded for nx-playground"
