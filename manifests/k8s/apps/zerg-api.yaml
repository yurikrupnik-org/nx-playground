# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/zerg/api/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zerg-api
    app.kubernetes.io/name: zerg-api
    app.kubernetes.io/component: api-server
    app.kubernetes.io/part-of: zerg
    app.kubernetes.io/version: '0.1.0'
  name: zerg-api
  namespace: zerg
spec:
  selector:
    matchLabels:
      app: zerg-api
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '8080'
        prometheus.io/path: /metrics
      labels:
        app: zerg-api
        app.kubernetes.io/name: zerg-api
        app.kubernetes.io/component: api-server
        app.kubernetes.io/part-of: zerg
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: REDIS_USERNAME
          valueFrom:
            secretKeyRef:
              key: username
              name: redis-redis-account-default
              optional: true
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: redis-redis-account-default
              optional: true
        envFrom:
        - configMapRef:
            name: zerg-shared-config
        - configMapRef:
            name: zerg-api-config
        - secretRef:
            name: zerg-shared-secrets
            optional: true
        - secretRef:
            name: zerg-api-secrets
            optional: true
        image: yurikrupnik/zerg-api:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: 8080
          periodSeconds: 10
          timeoutSeconds: 3
        name: zerg-api
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /ready
            port: 8080
          periodSeconds: 5
          timeoutSeconds: 2
        resources:
          limits:
            memory: '512Mi'
          requests:
            memory: '128Mi'
            cpu: '250m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 6
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: zerg-api
  namespace: zerg
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: zerg-api
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: zerg-api
  name: zerg-api
  namespace: zerg
spec:
  behavior:
    scaleDown:
      policies:
      - periodSeconds: 60
        type: Percent
        value: 10
      stabilizationWindowSeconds: 300
    scaleUp:
      policies:
      - periodSeconds: 15
        type: Percent
        value: 100
      - periodSeconds: 15
        type: Pods
        value: 4
      selectPolicy: Max
      stabilizationWindowSeconds: 0
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  - resource:
      name: memory
      target:
        averageUtilization: 80
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: zerg-api
---
apiVersion: v1
data:
  FLAGSMITH_API_URL: http://flagsmith-api.flagsmith.svc.cluster.local:8000/api/v1
  PORT: '8080'
  TASKS_SERVICE_ADDR: http://zerg-tasks:50051
  CORS_ALLOWED_ORIGIN: http://localhost:5206,https://127.0.0.1.nip.io:8443
  FRONTEND_URL: http://localhost:5206
  REDIRECT_BASE_URL: http://localhost:5206
kind: ConfigMap
metadata:
  name: zerg-api-config
  namespace: zerg
