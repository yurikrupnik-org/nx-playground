# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/terran/api/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: terran-api
    app.kubernetes.io/name: terran-api
    app.kubernetes.io/component: api-server
    app.kubernetes.io/part-of: terran
    app.kubernetes.io/version: '0.1.0'
  name: terran-api
  namespace: terran
spec:
  selector:
    matchLabels:
      app: terran-api
  template:
    metadata:
      labels:
        app: terran-api
        app.kubernetes.io/name: terran-api
        app.kubernetes.io/component: api-server
        app.kubernetes.io/part-of: terran
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: terran-api-config
        - secretRef:
            name: terran-api-secrets
            optional: true
        image: yurikrupnik/terran-api:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: 8080
          periodSeconds: 10
          timeoutSeconds: 3
        name: terran-api
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /ready
            port: 8080
          periodSeconds: 5
          timeoutSeconds: 2
        resources:
          limits:
            memory: '512Mi'
          requests:
            memory: '128Mi'
            cpu: '250m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 6
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: terran-api
  namespace: terran
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: terran-api
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: terran-api
  name: terran-api
  namespace: terran
spec:
  behavior:
    scaleDown:
      policies:
      - periodSeconds: 60
        type: Percent
        value: 10
      stabilizationWindowSeconds: 300
    scaleUp:
      policies:
      - periodSeconds: 15
        type: Percent
        value: 100
      - periodSeconds: 15
        type: Pods
        value: 4
      selectPolicy: Max
      stabilizationWindowSeconds: 0
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  - resource:
      name: memory
      target:
        averageUtilization: 80
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: terran-api
---
apiVersion: v1
data:
  PORT: '8080'
  RUST_LOG: info
kind: ConfigMap
metadata:
  name: terran-api-config
  namespace: terran
