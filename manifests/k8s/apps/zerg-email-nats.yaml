# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/zerg/email-nats/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zerg-email-nats
    app.kubernetes.io/name: zerg-email-nats
    app.kubernetes.io/component: email-worker
    app.kubernetes.io/part-of: zerg
    app.kubernetes.io/version: '0.1.0'
  name: zerg-email-nats
  namespace: zerg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zerg-email-nats
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '8081'
        prometheus.io/path: /metrics
      labels:
        app: zerg-email-nats
        app.kubernetes.io/name: zerg-email-nats
        app.kubernetes.io/component: email-worker
        app.kubernetes.io/part-of: zerg
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: APP_ENV
          value: info
        envFrom:
        - configMapRef:
            name: zerg-shared-config
        - configMapRef:
            name: zerg-email-nats-config
        - secretRef:
            name: zerg-email-nats-secrets
            optional: true
        image: yurikrupnik/zerg-email-nats:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: 8081
          periodSeconds: 10
          timeoutSeconds: 3
        name: zerg-email-nats
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /ready
            port: 8081
          periodSeconds: 5
          timeoutSeconds: 2
        resources:
          limits:
            memory: '256Mi'
          requests:
            memory: '64Mi'
            cpu: '100m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 6
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: zerg-email-nats
  namespace: zerg
spec:
  ports:
  - name: http
    port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: zerg-email-nats
  type: ClusterIP
---
apiVersion: v1
data:
  HEALTH_PORT: '8081'
  EMAIL_FROM_ADDRESS: noreply@zerg.local
  EMAIL_FROM_NAME: Zerg Dev
  SMTP_HOST: mailhog.zerg-dev.svc.cluster.local
  SMTP_PORT: '1025'
kind: ConfigMap
metadata:
  name: zerg-email-nats-config
  namespace: zerg
