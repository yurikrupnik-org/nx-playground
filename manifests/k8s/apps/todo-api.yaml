# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/todo/api/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: todo-api
    app.kubernetes.io/name: todo-api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: todo
    app.kubernetes.io/version: '0.1.0'
  name: todo-api
  namespace: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-api
  template:
    metadata:
      labels:
        app: todo-api
        app.kubernetes.io/name: todo-api
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: todo
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              key: database_url
              name: todo-api-env
              optional: false
        - name: NATS_URL
          value: nats://nats:4222
        - name: APP_ENV
          value: development
        image: yurikrupnik/todo-api:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: http
          periodSeconds: 10
          timeoutSeconds: 3
        name: todo-api
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: http
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          limits:
            memory: '128Mi'
            cpu: '500m'
          requests:
            memory: '32Mi'
            cpu: '50m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 12
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: todo-api
  namespace: todo
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: todo-api
  type: ClusterIP
---
apiVersion: v1
kind: Secret
metadata:
  name: todo-api-env
  namespace: todo
stringData:
  database_url: postgres://todo:todo@postgres:5432/todo
type: Opaque
