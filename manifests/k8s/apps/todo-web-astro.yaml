# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/todo/web-astro/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: todo-web-astro
    app.kubernetes.io/name: todo-web-astro
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: todo
    app.kubernetes.io/version: '0.1.0'
  name: todo-web-astro
  namespace: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-web-astro
  template:
    metadata:
      labels:
        app: todo-web-astro
        app.kubernetes.io/name: todo-web-astro
        app.kubernetes.io/component: frontend
        app.kubernetes.io/part-of: todo
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: HOST
          value: '0.0.0.0'
        - name: PORT
          value: '8080'
        - name: TODO_API_URL
          value: http://todo-api:8080
        image: yurikrupnik/todo-web-astro:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: http
          periodSeconds: 10
          timeoutSeconds: 3
        name: todo-web-astro
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: http
          periodSeconds: 5
          timeoutSeconds: 2
        resources:
          limits:
            memory: '512Mi'
            cpu: '1000m'
          requests:
            memory: '192Mi'
            cpu: '100m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
        startupProbe:
          failureThreshold: 12
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: todo-web-astro
  namespace: todo
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: todo-web-astro
  type: ClusterIP
