# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/zerg/tasks/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zerg-tasks
    app.kubernetes.io/name: zerg-tasks
    app.kubernetes.io/component: grpc-server
    app.kubernetes.io/part-of: zerg
    app.kubernetes.io/version: '0.1.0'
  name: zerg-tasks
  namespace: zerg
spec:
  selector:
    matchLabels:
      app: zerg-tasks
  template:
    metadata:
      labels:
        app: zerg-tasks
        app.kubernetes.io/name: zerg-tasks
        app.kubernetes.io/component: grpc-server
        app.kubernetes.io/part-of: zerg
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: REDIS_USERNAME
          valueFrom:
            secretKeyRef:
              key: username
              name: redis-redis-account-default
              optional: true
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: redis-redis-account-default
              optional: true
        envFrom:
        - configMapRef:
            name: zerg-shared-config
        - configMapRef:
            name: zerg-tasks-config
        - secretRef:
            name: zerg-tasks-secrets
            optional: false
        image: yurikrupnik/zerg-tasks:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          grpc:
            port: 50051
          periodSeconds: 10
          timeoutSeconds: 3
        name: zerg-tasks
        ports:
        - containerPort: 50051
          name: grpc
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          grpc:
            port: 50051
          periodSeconds: 5
          timeoutSeconds: 2
        resources:
          limits:
            memory: '512Mi'
          requests:
            memory: '128Mi'
            cpu: '250m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 6
          grpc:
            port: 50051
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: zerg-tasks
  namespace: zerg
spec:
  ports:
  - name: grpc
    port: 50051
    protocol: TCP
    targetPort: 50051
  selector:
    app: zerg-tasks
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: zerg-tasks
  name: zerg-tasks
  namespace: zerg
spec:
  behavior:
    scaleDown:
      policies:
      - periodSeconds: 60
        type: Percent
        value: 10
      stabilizationWindowSeconds: 300
    scaleUp:
      policies:
      - periodSeconds: 15
        type: Percent
        value: 100
      - periodSeconds: 15
        type: Pods
        value: 4
      selectPolicy: Max
      stabilizationWindowSeconds: 0
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  - resource:
      name: memory
      target:
        averageUtilization: 80
        type: Utilization
    type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: zerg-tasks
---
apiVersion: v1
data:
  GRPC_HOST: '0.0.0.0'
  GRPC_PORT: '50051'
kind: ConfigMap
metadata:
  name: zerg-tasks-config
  namespace: zerg
