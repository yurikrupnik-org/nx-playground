# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/todo/worker/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: todo-worker
    app.kubernetes.io/name: todo-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: todo
    app.kubernetes.io/version: '0.1.0'
  name: todo-worker
  namespace: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-worker
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '8091'
        prometheus.io/path: /metrics
      labels:
        app: todo-worker
        app.kubernetes.io/name: todo-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: todo
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: NATS_URL
          value: nats://nats:4222
        - name: APP_ENV
          value: development
        image: yurikrupnik/todo-worker:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: health
          periodSeconds: 10
          timeoutSeconds: 3
        name: todo-worker
        ports:
        - containerPort: 8091
          name: health
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /readyz
            port: health
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          limits:
            memory: '128Mi'
            cpu: '250m'
          requests:
            memory: '32Mi'
            cpu: '50m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 12
          httpGet:
            path: /healthz
            port: health
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 15
      volumes:
      - emptyDir: {}
        name: tmp
