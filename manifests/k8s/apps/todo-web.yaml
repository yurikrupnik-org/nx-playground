# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/todo/web/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: todo-web
    app.kubernetes.io/name: todo-web
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: todo
    app.kubernetes.io/version: '0.1.0'
  name: todo-web
  namespace: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-web
  template:
    metadata:
      labels:
        app: todo-web
        app.kubernetes.io/name: todo-web
        app.kubernetes.io/component: frontend
        app.kubernetes.io/part-of: todo
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: UPSTREAM_HOST
          valueFrom:
            configMapKeyRef:
              key: upstream_host
              name: todo-web-config
        - name: UPSTREAM_PORT
          valueFrom:
            configMapKeyRef:
              key: upstream_port
              name: todo-web-config
        - name: UPSTREAM_NAME
          valueFrom:
            configMapKeyRef:
              key: upstream_name
              name: todo-web-config
        image: yurikrupnik/todo-web:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: http
          periodSeconds: 10
          timeoutSeconds: 3
        name: todo-web
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: http
          periodSeconds: 5
          timeoutSeconds: 2
        resources:
          limits:
            memory: '64Mi'
            cpu: '100m'
          requests:
            memory: '32Mi'
            cpu: '50m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 101
        startupProbe:
          failureThreshold: 4
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /etc/nginx/conf.d
          name: nginx-conf-d
        - mountPath: /var/cache/nginx
          name: nginx-cache
        - mountPath: /var/run
          name: nginx-run
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 101
        runAsGroup: 101
        runAsNonRoot: true
        runAsUser: 101
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: nginx-conf-d
      - emptyDir: {}
        name: nginx-cache
      - emptyDir: {}
        name: nginx-run
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: todo-web
  namespace: todo
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: todo-web
  type: ClusterIP
---
apiVersion: v1
data:
  upstream_host: todo-api
  upstream_name: backend
  upstream_port: '8080'
kind: ConfigMap
metadata:
  name: todo-web-config
  namespace: todo
