# GENERATED by `butler k8s gen` — DO NOT EDIT BY HAND.
# Source: apps/todo/web-htmx/butler.toml [workload], rendered by oci://docker.io/yurikrupnik/app?tag=0.1.2 (env dev).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: todo-web-htmx
    app.kubernetes.io/name: todo-web-htmx
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: todo
    app.kubernetes.io/version: '0.1.0'
  name: todo-web-htmx
  namespace: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-web-htmx
  template:
    metadata:
      labels:
        app: todo-web-htmx
        app.kubernetes.io/name: todo-web-htmx
        app.kubernetes.io/component: frontend
        app.kubernetes.io/part-of: todo
        app.kubernetes.io/version: '0.1.0'
    spec:
      containers:
      - env:
        - name: TODO_WEB_HTMX_PORT
          value: '8080'
        - name: TODO_API_URL
          value: http://todo-api:8080
        image: yurikrupnik/todo-web-htmx:dev
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: http
          periodSeconds: 10
          timeoutSeconds: 3
        name: todo-web-htmx
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: http
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          limits:
            memory: '128Mi'
            cpu: '500m'
          requests:
            memory: '32Mi'
            cpu: '50m'
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
        startupProbe:
          failureThreshold: 12
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      restartPolicy: Always
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: v1
kind: Service
metadata:
  name: todo-web-htmx
  namespace: todo
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: todo-web-htmx
  type: ClusterIP
