apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: db-timeseries-subscription
spec:
  pubsubname: pubsub-nats
  topic: db.timeseries
  routes:
    default: /events/db-timeseries
  scopes:
    - db-worker-influxdb
