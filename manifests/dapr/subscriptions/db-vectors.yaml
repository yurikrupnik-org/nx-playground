apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: db-vectors-subscription
spec:
  pubsubname: pubsub-nats
  topic: db.vectors
  routes:
    default: /events/db-vectors
  scopes:
    - db-worker-qdrant
