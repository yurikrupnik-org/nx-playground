apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: db-tasks-subscription
spec:
  pubsubname: pubsub-nats
  topic: db.tasks
  routes:
    default: /events/db-tasks
  scopes:
    - db-worker-postgres
    - db-worker-mongo
