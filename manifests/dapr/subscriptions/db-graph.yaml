apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: db-graph-subscription
spec:
  pubsubname: pubsub-nats
  topic: db.graph
  routes:
    default: /events/db-graph
  scopes:
    - db-worker-neo4j
